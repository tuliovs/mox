#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../marko-starter/bin/marko-starter" "$@"
  ret=$?
else 
  node  "$basedir/../marko-starter/bin/marko-starter" "$@"
  ret=$?
fi
exit $ret
